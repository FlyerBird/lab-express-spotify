<div>
    {{#each items}}
        <h2>name: {{ name }}</h2>
        <button> View Album </button>
        {{#each images}}
        <p>
           <img src="{{url}}" alt="{{name}}" >
        </p>
    {{/each}}
    {{/each}}
</div>